# Example .env file for rust-bitcoin-indexer
# Copy this to `.env` and edit values as appropriate.
# Keep paths absolute (tilde ~ is not expanded).

# Postgres connection string
# - The user should be a role with appropriate privileges.
# - The hostname should match the Postgres server certificate when using TLS.
DATABASE_URL=postgres://bitcoin-indexer@db.example.com:5432/bitcoin

# Bitcoin Core JSON-RPC endpoint
# Adjust port to your node setup (e.g., 8332 for mainnet, 18443 for regtest)
NODE_RPC_URL=http://rpcuser:rpcpassword@127.0.0.1:8332

# Optional TLS/mTLS settings for Postgres (PEM-encoded files)
# Uncomment and set these to enable TLS with client certificates.
# PGSSLROOTCERT=/absolute/path/to/ca.example.com.pem
# PGSSLCERT=/absolute/path/to/client.example.com.pem
# PGSSLKEY=/absolute/path/to/client.example.com.key

# Optional: control logging verbosity
# RUST_LOG=bitcoin_indexer=info
